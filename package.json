{
  "name": "changelog-release-slack-notify",
  "version": "0.1.0",
  "description": "GitHub action to send release note to slack",
  "main": "src/index.js",
  "scripts": {
    "build": "ncc build src/index.js"
  },
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@actions/http-client": "^2.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.5"
  }
}
